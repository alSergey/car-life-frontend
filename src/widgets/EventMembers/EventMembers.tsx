import { Group } from "@vkontakte/vkui";
import React from "react";

interface Props {
	id: number;
}

export const EventMembers: React.FC<Props> = ({}) => {
	return <Group>Здесь скоро будут участники</Group>;
};
